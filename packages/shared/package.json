{
    "name": "capgo-update-lite-shared",
    "version": "0.0.0",
    "private": true,
    "type": "module",
    "description": "Code shared between the capgo-update-lite server and CLI. Workspace-internal — do not publish.",
    "exports": {
        ".": "./src/index.ts",
        "./semver": "./src/semver.ts"
    },
    "scripts": {
        "check": "tsc --noEmit",
        "test": "vitest run",
        "test:watch": "vitest"
    },
    "devDependencies": {
        "typescript": "^5.9.0",
        "vitest": "^4.1.5"
    }
}
